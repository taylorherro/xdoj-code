#include<stdio.h>
#include<stdlib.h>
typedef struct node {
	int data;
	struct node* next;
}linklsit;
linklist* creat(int n)
{
	linklist *head,*r,*s;
	head = (linklist*)malloc(sizeof(linklist));
	r = head;
	for (int i = 0; i < n; i++)
	{
		scanf("%d", &a);
	}
}