#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<malloc.h>
typedef struct node {
	int data;
	struct node* lchild;
	struct node* rchild;
}bitree;
bitree* insert(bitree* t, bitree* s)
{

}
bitree* create(int num)
{

}
int main()
{
	int num;
	scanf("%d", &num);
	bitree* tree = create(num);
	return 0;
}